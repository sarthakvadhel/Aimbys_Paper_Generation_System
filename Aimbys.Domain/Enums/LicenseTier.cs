namespace Aimbys.Domain.Enums;

/// <summary>
/// Subscription tier sold to an institute. Mirrors the values shown in the
/// React reference (`InstituteManagement.tsx`).
/// </summary>
public enum LicenseTier
{
    Standard = 0,
    Premium = 1,
    Enterprise = 2
}
